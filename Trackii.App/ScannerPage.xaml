<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="Trackii.App.ScannerPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:zxing="clr-namespace:ZXing.Net.Maui.Controls;assembly=ZXing.Net.Maui.Controls"
    Title="Scanner">
    <Grid RowDefinitions="2*,Auto,Auto" Padding="16" RowSpacing="12">
        <Frame Grid.Row="0" CornerRadius="16" HasShadow="False" Padding="0">
            <zxing:CameraBarcodeReaderView
                x:Name="BarcodeReader"
                CameraLocation="Rear"
                IsDetecting="True"
                BarcodesDetected="OnBarcodesDetected" />
        </Frame>

        <VerticalStackLayout Grid.Row="1" Spacing="8">
            <Label Text="No. de orden" FontAttributes="Bold" />
            <Entry x:Name="OrderEntry" Placeholder="Se llena con 7 dígitos" />
        </VerticalStackLayout>

        <VerticalStackLayout Grid.Row="2" Spacing="8">
            <Label Text="No. de parte" FontAttributes="Bold" />
            <Entry x:Name="PartEntry" Placeholder="Se llena con cualquier otro código" />
        </VerticalStackLayout>
    </Grid>
</ContentPage>
